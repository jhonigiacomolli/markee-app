import styled from 'styled-components'

const ResultWrapper = styled.div`
    grid-area:  html;
`
const Title = styled.h1`
    font-family: 'DM Sans', sans-serif;
    font-size: 3.2rem;
    font-weight: 700;
    margin: 10px 0;
`
const Text = styled.p`
    font-family: 'DM Sans', sans-serif;
    font-size: 1.8rem;
    font-weight: 400;
    margin: 0;
`

export { ResultWrapper, Title, Text }
